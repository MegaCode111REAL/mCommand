# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  check:
    name: Check code before deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install syntax checkers
        run: |
          npm install --no-save --no-package-lock prettier@3 eslint@9 html-validate@10

      - name: Check formatting
        run: npx prettier . --check

      - name: Check JavaScript syntax
        run: |
          find . -type f -name "*.js" -not -path "./node_modules/*" -print0 |
            xargs -0 -r -n1 node --check

      - name: Check JSON syntax
        run: |
          find . -type f -name "*.json" -not -path "./node_modules/*" -print0 |
            xargs -0 -r -n1 node -e 'const fs=require("fs"); JSON.parse(fs.readFileSync(process.argv[1],"utf8"));' 

      - name: Check HTML syntax
        run: npx html-validate "**/*.html"

  deploy:
    name: Deploy to GitHub Pages
    needs: check
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
